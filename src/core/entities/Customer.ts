export interface Customer {
  mnmua: string;                  // Mã người mua
  mst: string;                    // Mã số thuế
  tnmua: string;                  // Tên người mua
  email: string;                  // Email
  ten: string;                    // Tên liên hệ
  dchi: string;                   // Địa chỉ
  stknmua: string;                // Số tài khoản người mua
  tnhmua: string;                 // Tên ngân hàng người mua
  sdtnmua: string;                // Số điện thoại người mua
}
